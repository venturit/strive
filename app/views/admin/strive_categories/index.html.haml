.row
  .col-md-6
    %h1.h3.mb-0.text-gray-800 Strive Categories
  -if current_user.admin? 
    .col-md-6.text-right.mb-4
      = link_to 'New Strive Category', new_admin_strive_category_path, class:"btn btn-primary"
.row
  .card-deck
    - @strive_categories.each do |strive_category|
      .col-md-3.mb-8.p-10
        .card{:style => "width: 16rem;"}
          .card-body
            %h5.card-title #{strive_category.name}
            %p.card-text  #{strive_category.description}
          .card-footer.text-right
            -if current_user.admin? 
              = link_to 'Edit', edit_admin_strive_category_path(strive_category), class:"btn btn-primary"